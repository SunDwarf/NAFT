# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py33,py34,py35,py36,nightly
[testenv]
usedevelop = true
deps=pytest
    pytest-cov
    typing
commands=py.test --cov=naft -rfEsxXw -v --strict
setenv =
    PYTHONPATH = {toxinidir}
